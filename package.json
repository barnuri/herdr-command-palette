{
    "name": "herdr-command-palette",
    "version": "0.1.0",
    "description": "F1-style command palette for herdr — fuzzy-search and run every action from every installed plugin",
    "author": "barnuri",
    "license": "MIT",
    "type": "commonjs",
    "engines": {
        "node": ">=22.0.0"
    },
    "scripts": {
        "test": "node --test \"tests/*.test.js\""
    },
    "keywords": ["herdr", "herdr-plugin", "command-palette", "fuzzy", "terminal"]
}
